<Application x:Class="BudgetPlanner.App.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:BudgetPlanner.App"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <SolidColorBrush x:Key="bg" Color="#191724"></SolidColorBrush>
        <SolidColorBrush x:Key="layer" Color="#1f1d2e"></SolidColorBrush>
        <SolidColorBrush x:Key="fg" Color="#e0def4"></SolidColorBrush>


        <Style x:Key="list" TargetType="ListView">
            <Setter Property="Foreground" Value="{StaticResource fg}"/>
            <Setter Property="Background" Value="{StaticResource layer}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>


        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Foreground" Value="{StaticResource fg}"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>


        <Style TargetType="{x:Type TextBox}">
            <Setter Property="Foreground" Value="{StaticResource fg}"/>
            <Setter Property="Background" Value="{StaticResource layer}"/>
        </Style>

        <Style x:Key="h1" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="Foreground" Value="{StaticResource fg}"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style x:Key="h2" TargetType="TextBlock">
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Foreground" Value="{StaticResource fg}"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>
        <Style x:Key="p" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Foreground" Value="{StaticResource fg}"/>
            <Setter Property="Background" Value="Transparent"/>
        </Style>


        <Style TargetType="{x:Type Button}">
            <Setter Property="Foreground" Value="{StaticResource fg}"/>
            <Setter Property="Background" Value="{StaticResource layer}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                        Background="{TemplateBinding Background}" 
                        BorderBrush="{TemplateBinding BorderBrush}" 
                        BorderThickness="{TemplateBinding BorderThickness}" 
                        CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource layer}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource layer}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="card" TargetType="StackPanel">
            <Setter Property="Background" Value="{StaticResource layer}"/>
        </Style>

        <Style x:Key="card-border" TargetType="Border">
            <Setter Property="BorderThickness" Value="8"/>
            <Setter Property="BorderBrush" Value="{StaticResource layer}"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Margin" Value="8"/>
        </Style>
    </Application.Resources>
</Application>

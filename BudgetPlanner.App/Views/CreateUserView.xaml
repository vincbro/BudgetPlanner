<UserControl x:Class="BudgetPlanner.App.Views.CreateUserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BudgetPlanner.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Border Style="{StaticResource card-border}">
            <StackPanel Style="{StaticResource card}" Orientation="Horizontal">
                <TextBlock>First name: </TextBlock>
                <TextBox MinWidth="150" Text="{Binding FirstName}"></TextBox>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource card-border}">
            <StackPanel Style="{StaticResource card}" Orientation="Horizontal">
                <TextBlock>Last name: </TextBlock>
                <TextBox MinWidth="150" Text="{Binding LastName}"></TextBox>
            </StackPanel>
        </Border>

        <Border Style="{StaticResource card-border}">
            <Button Click="CreateUserClicked">
                Create new user
            </Button>
        </Border>
    </StackPanel>
</UserControl>

<UserControl x:Class="BudgetPlanner.App.Views.UsersListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BudgetPlanner.App.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock TextAlignment="Center" Style="{StaticResource h1}">
            Welcome to Budget Planer
        </TextBlock>
        <TextBlock TextAlignment="Center" Style="{StaticResource h2}">
            Who are you?
        </TextBlock>
        <Border Style="{StaticResource card-border}">
            <ListView ItemsSource="{Binding Users}" SelectedItem="{Binding Path=SelectedUser}" Style="{StaticResource list}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding FirstName}"  Margin="0 8 0 8" Style="{StaticResource p}"/>
                            <TextBlock  Text=" " Margin="0 8 0 8" Style="{StaticResource p}"/>
                            <TextBlock Text="{Binding LastName}" Margin="0 8 0 8" Style="{StaticResource p}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>

        <Border Style="{StaticResource card-border}">
            <Button Click="SelectUser" IsEnabled="{Binding IsUserSelected}">
                Select user
            </Button>
        </Border>
        <Border Style="{StaticResource card-border}">
            <Button Click="CreateUserClicked">
                Create new user
            </Button>
        </Border>
    </StackPanel>
</UserControl>
